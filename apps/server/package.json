{
  "name": "server",
  "main": "src/index.ts",
  "type": "module",
  "scripts": {
    "build": "tsdown",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --sourcemap --bytecode ./src/index.ts --outfile server",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "hono": "^4.8.2",
    "@hono/node-server": "^1.14.4",
    "dotenv": "catalog:",
    "zod": "catalog:",
    "@view-port/db": "workspace:*"
  },
  "devDependencies": {
    "typescript": "catalog:",
    "tsx": "^4.19.2",
    "@types/node": "catalog:",
    "tsdown": "catalog:"
  }
}
